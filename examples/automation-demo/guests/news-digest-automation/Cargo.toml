[package]
name = "news-digest-automation"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
mango-automation-sdk = { path = "../../../../crates/kernel/automation-sdk" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[workspace]
