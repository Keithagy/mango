//! Compatibility wrapper for the automation control-plane surface.
//!
//! The concrete implementation lives in `mango-automations`; this crate keeps a
//! focused host-facing entrypoint at the historical orchestration path.

pub use mango_automations::*;
